machine:
  xcode:
    version: "8.0"

test:
  override:
    - set -o pipefail &&
      xcodebuild
        -project Demo.xcodeproj
        -scheme iOSTests
        -destination 'platform=iOS Simulator,name=iPhone 6'
        build test |
      xcpretty -c
